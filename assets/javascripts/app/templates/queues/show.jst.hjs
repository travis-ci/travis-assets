<h4>{{t "queue"}}: {{display}}</h4>
{{#collection tagName="ul" class="jobs" itemClass="job" contentBinding="jobs"}}
  {{content.repository.slug}}
  {{#if content.number}}
    #{{content.number}}
  {{/if}}
{{else}}
  {{t "no_job"}}
{{/collection}}

