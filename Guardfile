$: << 'lib'

guard 'assets' do
  watch(%r(^AssetFile))
  watch(%r(^assets))
end

# guard 'rspec', :version => 2 do
#   watch(%r(AssetFile)) { 'spec' }
#   watch(%r{^spec/.+_spec\\.rb$})
#   watch(%r{^lib/travis/(.+)\\.rb$}) { |m| "spec/#{m[1]}_spec.rb" }
#   watch('spec/spec_helper.rb')  { "spec" }
# end
